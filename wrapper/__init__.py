"""`audiochatty run` — the wrapper that replaces the old plugin channel.

Read `__main__.py` first: its docstring is the frozen local API that
`scripts/audiochat.py` is written against. See `README.md` in this directory for what this
process can do to your machine.
"""
